<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Theme -->
    <style name="Theme.TextLaunch" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="android:windowBackground">@color/background_dark</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:navigationBarColor">@color/background_dark</item>
    </style>

    <!-- Text Styles -->
    <style name="TextLaunch.Text.AppName" parent="TextAppearance.Material3.BodyMedium">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">14sp</item>
        <item name="fontFamily">monospace</item>
    </style>

    <style name="TextLaunch.Text.AppName.Small" parent="TextLaunch.Text.AppName">
        <item name="android:textSize">12sp</item>
    </style>

    <style name="TextLaunch.Text.AppName.Large" parent="TextLaunch.Text.AppName">
        <item name="android:textSize">16sp</item>
    </style>

    <!-- Card Style -->
    <style name="TextLaunch.Card" parent="Widget.Material3.CardView.Elevated">
        <item name="cardBackgroundColor">@color/background_surface</item>
        <item name="cardCornerRadius">8dp</item>
        <item name="cardElevation">0dp</item>
        <item name="strokeColor">@color/neon_cyan</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <!-- Button Style -->
    <style name="TextLaunch.Button" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">@color/neon_cyan</item>
        <item name="android:textSize">12sp</item>
        <item name="fontFamily">monospace</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>